#ifndef FLAGS_H
#define FLAGS_H

enum Bullet_shot{
    shot, not_shot
};

enum Living{
    alive, dead
};

enum Hit{
    hit, no_hit
};

enum player_no {
    first, second, winner, loser, unassigned
};

#endif // FLAGS_H
